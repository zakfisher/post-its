'use strict'

import CloseIcon from '../icons/close.jsx'

const Back = (props) => {
  return (
    <div className='back'>
      <CloseIcon {...props} />
      <p>The Back..</p>
    </div>
  )
}

export default Back
